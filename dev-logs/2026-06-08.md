# 开发日志 - 2026-06-08

## 今日完成
- [x] 阶段12：测试优化与发布准备（发布跳过，按用户要求）

### Block 1: 关键Bug修复
- [x] **blob URL内存泄漏** — 6处 `URL.createObjectURL()` 添加 `revokeObjectURL` 回收
  - `js/app.js`: 新增 `Game.revokeElementBlobURLs(element)` 辅助函数
  - `js/storage.js`: 新增 `revokePhotoURL(url)` 导出
  - `js/profile.js:194,223`: 壁纸预览 + 头像预览
  - `js/character.js:350,409,458`: 设定向导3处
  - `js/phone/phone-main.js:293`: 相册网格
- [x] **AI响应JSON解析崩溃** — sns.js 两处 `JSON.parse()` 添加局部 try/catch 保护
  - 新增 `getFallbackComments()` / `getMyPostFallbackComments()` 内部函数
- [x] **localStorage QuotaExceededError** — `storage.js:saveGame()` 添加 try/catch
  - 满时自动清理最旧存档槽重试
  - 新增 `_findOldestSaveSlot()` / `getStorageUsage()` 工具函数

### Block 2: 性能优化
- [x] **autoSave防抖** — `game-state.js`: 250ms debounce
  - 新增 `_dirty` / `_saveTimer` 状态追踪
  - 新增 `flushSave()` 用于关键路径（returnToTitle、endTurn、beforeunload）
  - 每回合从 ~15次 localStorage 写入降到 ~1次
- [x] **DEBUG门控** — `Game.DEBUG = false`
  - 全部17个JS文件中 64处 `console.log` 替换为 `Game.DEBUG && console.log`
  - 保留 `console.warn`（27处）和 `console.error`（5处）
- [x] **SNS评论缓存上限** — `sns.js`: MAX_CACHE_SIZE = 50，_trimCache() FIFO淘汰
- [x] **手机模块DOM缓存** — `sns.js`/`chat.js`/`news.js` 添加 `_getContentEl()` 缓存
  - `phone-main.js:closeApp()` 调用各模块 `resetCache()` 重置缓存

### Block 3: 测试验证
- [x] 存档兼容测试 — migrate() 从 v0.13.0 → v0.14.0 正常
- [x] 全流程测试清单编写
- [x] 浏览器兼容说明（Safari 14.1+, Chrome 90+）

### Block 4: 收尾
- [x] 版本号: `0.13.0` → `0.14.0`
- [x] 阶段字符串: 修正为 `阶段12 ➡️ 测试优化与发布准备`
- [x] README.md: 添加系统要求、更新日志、隐私说明
- [x] CLAUDE.md: 更新当前开发阶段表
- [x] docs/04-开发执行步骤.md: 标记阶段12完成

## 变更统计

- 修改文件: 17个JS + 1个HTML + 3个文档 = 21个文件
- 新增代码: ~120行（工具函数 + 防抖逻辑 + 错误处理）
- 修改代码: ~60行（console.log门控 + DOM缓存 + revoke调用）

## 遇到的问题
- `replace_all` 替换 `document.getElementById('phone-app-content')` 时误替换了 `_getContentEl()` 函数内部的同名调用，导致递归。已手动修复 chat.js、sns.js、news.js 三处。
- app.js 出现双 `Game.DEBUG &&` 门控（手动编辑 + sed 重复替换），已修复。

## 今日完成（续）
- [x] 阶段8补充：亲友社交圈（闺蜜态度/家人反应/队友态度）
- [x] 阶段8补充：公司干涉完整版（禁爱令/封口协议/限制接触）
- [x] 6个新现实事件全部集成到回合结束流程
- [x] 禁爱令/限制接触贯穿约会行动好感度计算

### 阶段8补充详情

**亲友社交圈（+3事件类型）**
- 👯 闺蜜态度：每5-8回合30%触发，根据嫌疑度变化对话内容
  - 3种回应（全盘托出/含糊其辞/分享近况等），影响压力/嫌疑度
- 👨‍👩‍👧 家人反应：嫌疑度≥30且有恋爱关系时15%触发
  - 嫌疑度分3档台词，3种回应（坦诚/闪烁/坚定等）
  - 反应结果存入 `familyReactions` 状态
- 🎤 队友态度：恋爱/结婚后15%触发（60%支持/40%警惕）
  - 影响好感度、魅力、嫌疑度
  - 态度存入 `teammateAttitudes` 状态

**公司干涉完整版（+3事件类型）**
- 🏢 禁爱令：嫌疑度≥50有恋爱关系时12%触发
  - 3选项：配合（锁3回合）/ 协商（粉丝-10~30锁1回合）/ 对抗（嫌疑度+10）
  - 状态追踪：`datingBans` 存储到期回合
  - 约会时检测禁爱令→警告+嫌疑度惩罚
- 📄 封口协议：嫌疑度≥40有恋爱关系时18%触发
  - 签署：压力+10嫌疑度-10但SNS受限 / 暂缓 / 拒绝
  - 状态追踪：`gagOrders` 标记
- 🚧 限制接触：嫌疑度≥45有恋爱关系时15%触发（有禁爱令/封口协议→25%）
  - 接受：好感获取减半3回合 / 找方式：体力-20 / 抗议：压力+5嫌疑度+3
  - 状态追踪：`contactRestrictions` 存储等级和到期回合
  - 好感计算集成：1级-30% / 2级-50% / 3级-70%

### 变更的文件
- `js/game-state.js` — +17个状态追踪函数（禁爱令/封口协议/限制接触/家人反应/队友态度/闺蜜冷却）+ migrate补齐
- `js/reality.js` — +6个事件检测函数 + 对应resolve函数 + 通用showSocialModal弹窗
- `js/turn.js` — 回合结束新增6个事件触发 + 禁爱令约会警告 + 限制接触好感惩罚

## 当前阶段
阶段12：测试优化与发布准备 — 8/8项完成 ✅（GitHub Pages发布跳过）
阶段8：现实元素系统 — 全部补充完成 ✅（6/6子系统全部就位）

版本: v0.14.0
